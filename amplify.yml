version: 1
backend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npm install -g ampx || (echo "Failed to install ampx" && exit 1)
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID || (cat /root/.npm/_logs/*-debug-0.log && exit 1)
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.9.0
        - nvm use 20.9.0
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*